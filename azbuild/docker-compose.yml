version: '3.4'

services:
  shoppingexpo:
    image: lawisnie/azbuild:20211016.3
    container_name: azbuild
    env_file:
      - ~/.secrets/azbuild.env
    volumes:
      - ~/.kube/:/home/azuser/.kube
    restart: always
    build:
      context: .
      dockerfile: ./Dockerfile
